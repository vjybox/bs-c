# apps/web currently requires no environment variables: API calls are made
# to the relative path /api, which is proxied to the api service both by
# Vite's dev server (vite.config.ts) and by nginx in the production image
# (apps/web/nginx.conf). This file exists as a placeholder for future config.
